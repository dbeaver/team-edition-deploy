# Default values for dbeaverTE.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# dbeaver-te general values for all TE services
dbeaverTE:
  imageTag: "24.2.0"
  pullPolicy: Always
  # pullCredsName - name of a secret config map that contains docker repo auths
  # pullCredsName: regcred

# Ingress Values
# Supported ingress controllers: aws-alb, nginx, haproxy
ingressController: nginx
# Domain name for ingress endpoint
dbeaverTEBaseDomain: "dbeaver.local"
# LoadBallancer secure mode. Possible values - http, https
httpScheme: http
# If httpScheme is https.
# Domain certificates must be presented in root folder of chart with names:
httpsCertPath: "ingressSsl"
# certificate: <httpsCertPath>/fullchain.pem
# certificate key: <httpsCertPath>/privkey.pem
# If you keep certificate in AWS Certificate Manager and use aws-alb set your certificate ARN
# Domain certificates must equal dbeaverTEBaseDomain
certificateARN: ""


crossServiceCertPath: "crossSsl"
secretCertPath: "secretSsl"
# Service depend values
dbeaver_te_web:
  replicaCount: 1
  image: dbeaver/team-te
dbeaver_te_dc:
  replicaCount: 1
  image: dbeaver/team-dc
dbeaver_te_rm:
  replicaCount: 1
  image: dbeaver/team-rm
dbeaver_te_qm:
  replicaCount: 1
  image: dbeaver/team-qm
dbeaver_te_tm:
  replicaCount: 1
  image: dbeaver/team-tm

# Cloud storage configuration 
# By default is None, it means not cloud deployment
# AWS - type: efs storageClassName: efs-sc
# GCP - type: filestore storageClassName: filestore-sc
cloudProvider: None  # Options: None, aws, gcp
storage:
  type: efs
  storageClassName: "efs-sc"
  efs:
    fileSystemId: ""

backend:
  internal_db: true
  dbeaver_te_db_driver: postgres-jdbc
  dbeaver_te_db_url: jdbc:postgresql://postgres:5432/cloudbeaver
  dbeaver_te_db_user: postgres
  dbeaver_te_db_password: StR0NgP2sSw0rD
