apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: dbeaver-te-web
  name: dbeaver-te-web
spec:
  replicas: {{ .Values.dbeaver_te_web.replicaCount }}
  selector:
    matchLabels:
      app: dbeaver-te-web
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        date: "{{ now | unixEpoch }}"
        {{ .Release.Name }}-net: "true"
        app: dbeaver-te-web
    spec:
      # securityContext:
      #     runAsUser: 1000
      #     runAsGroup: 1000
      #     fsGroup: 1000
      #     fsGroupChangePolicy: "Always"
      containers:
        - image: "{{ .Values.dbeaver_te_web.image }}:{{ .Values.dbeaverTE.imageTag | default .Chart.AppVersion }}"
          imagePullPolicy: "{{ .Values.dbeaverTE.pullPolicy }}"
          name: dbeaver-te-web
          env:
            - name: DBEAVER_TE_DC_SERVER_URL
              value: "http://dbeaver-te-dc:8970/dc"
          ports:
            - containerPort: 8978
          resources: {}
          volumeMounts:
            - name: cross-ssl-cert
              mountPath: /opt/cloudbeaver/conf/certificates
              readOnly: true
      {{- if .Values.dbeaverTE.pullCredsName }}
      imagePullSecrets:
        - name: {{ .Values.dbeaverTE.pullCredsName }}
      {{- end }}
      hostname: dbeaver-te-web
      restartPolicy: Always
      volumes:
        - name: cross-ssl-cert
          secret:
            secretName: {{ .Release.Name }}-crossservice-ssl
            items:
              - key: tls.crt
                path: dc-cert.crt